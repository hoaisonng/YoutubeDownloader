<Window x:Class="YoutubeDownloaderWpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Đăng nhập Youtube (Để lấy Cookies)" Height="600" Width="900" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#f0f0f0" Padding="10">
            <StackPanel>
                <TextBlock Text="Hướng dẫn: Đăng nhập vào tài khoản Youtube của bạn bên dưới. Sau khi đăng nhập xong, bấm nút 'Lưu Cookies' để tool sử dụng." TextWrapping="Wrap"/>
                <Button Name="btnSave" Content="ĐÃ ĐĂNG NHẬP XONG - LƯU COOKIES" Click="SaveCookies_Click" Margin="0,10,0,0" Height="40" Background="#FF0000" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <wv2:WebView2 Name="webView" Grid.Row="1"/>
    </Grid>
</Window>