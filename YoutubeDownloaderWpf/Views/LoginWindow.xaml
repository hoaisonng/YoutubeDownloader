<Window x:Class="YoutubeDownloaderWpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Đăng nhập Youtube (Để tải video Member)" Height="600" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#FFF8E1" Padding="10" BorderBrush="#FFB74D" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Sau khi đăng nhập thành công, hãy bấm nút này -> " VerticalAlignment="Center" FontWeight="Bold"/>
                <Button Content="LƯU COOKIES &amp; ĐÓNG" Click="SaveCookies_Click" Padding="15,5" Background="#4CAF50" Foreground="White" FontWeight="Bold" Margin="5,0,0,0"/>
            </StackPanel>
        </Border>

        <wv2:WebView2 Name="webView" Grid.Row="1" Source="https://accounts.google.com/ServiceLogin?service=youtube"/>
    </Grid>
</Window>